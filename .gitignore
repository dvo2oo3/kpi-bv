# Cấu hình thật chứa mật khẩu DB — KHÔNG đưa lên GitHub.
# Dùng config.example.php làm mẫu; bản thật chỉ nằm trên server + máy cá nhân.
/htdocs/app/config.php

# CSDL thử nghiệm cục bộ
*.sqlite
*.sqlite3

# Bản dump dữ liệu thật để đưa lên VPS (có hash mật khẩu) — không commit
/_local/qlbv_data_mysql.sql
